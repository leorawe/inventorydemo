/**
 * @file
 * demo_subtheme behaviors.
 */

(function ($, Drupal) {
  "use strict";

  /**
   * Behavior description.
   */
  Drupal.behaviors.demoSubtheme = {
    attach: function (context, settings) {
      // My custom js.
    },
  };
})(jQuery, Drupal);
